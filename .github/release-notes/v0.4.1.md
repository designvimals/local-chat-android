## Strict chat presence

- Reports `Online` only while the chat screen is visible in the foreground.
- Reports `Available` while the phone is connected to the relay but the chat is closed, backgrounded, or locked.
- Keeps relay file sharing and queued message delivery available while the phone is merely `Available`.
