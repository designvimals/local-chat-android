{
  "name": "private-chat-storage-poc",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "backend",
    "web",
    "shared/api-contracts"
  ],
  "scripts": {
    "dev:backend": "npm --workspace backend run dev",
    "dev:web": "npm --workspace web run dev",
    "build": "npm --workspace shared/api-contracts run build && npm --workspace backend run build && npm --workspace web run build",
    "check": "npm --workspace shared/api-contracts run check && npm --workspace backend run check && npm --workspace web run check"
  }
}
